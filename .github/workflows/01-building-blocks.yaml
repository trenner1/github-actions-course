name: 01 - Building Blocks
on:
  - push
  - workflow_dispatch

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello World"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: I will fail
        run: |
          echo "I will fail"
          exit 1
      
      - name: Say echo-goodbye
        run: echo "Goodbye"
